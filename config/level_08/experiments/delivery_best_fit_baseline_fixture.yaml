extends: ../runtime_candidate.yaml
project:
  algorithm_id: extreme_point_best_fit_delivery_baseline_fixture
paths:
  raw_items_csv: data/raw/level_08/delivery_best_fit_ab_items.csv
  processed_dir: data/processed/level_08/delivery_best_fit_ab_baseline
  manifest_json: data/processed/level_08/delivery_best_fit_ab_baseline/latest_manifest.json
containers:
  - {container_id: C1, length_mm: 250, width_mm: 80, height_mm: 60, max_weight_kg: 100, cost: 100, availability: 1}
fixture:
  fixture_id: level_08_delivery_best_fit_ab_v1
  required_item_count: 2
  required_container_count: 1
  required_environment: local
  required_item_selection_strategy: prefix
  required_item_selection_seed: null
