# Dùng cùng fleet cap/deadline với suite control để A/B KPI không bị lệch input.
extends: mes_inventory_gate_500_local.yaml
container_search:
  secondary_search_score:
    enabled: true
    complete_first_cardinality_portfolio: true
