extends: mes_inventory_gate_1000_local.yaml

# KPI phụ chỉ là tie-break sau objective chính thức; không thay đổi policy support.
container_search:
  secondary_search_score:
    enabled: true
    complete_first_cardinality_portfolio: true
