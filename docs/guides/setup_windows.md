# Windows setup

Run `python -m venv .venv`, activate with `.\.venv\Scripts\Activate.ps1`, then `pip install -r requirements.txt` and `pip install -e .`. If activation is blocked, use `Set-ExecutionPolicy -Scope Process Bypass` or call `.venv\Scripts\python.exe` directly.
