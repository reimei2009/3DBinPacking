# Debugging

For missing files, run the command at project root and regenerate processed data. For missing `milp`, upgrade SciPy to the declared range. A tiny negative coordinate below configured tolerance is normalized to zero. Floating items are allowed in Level 1 and must not be treated as overlaps or automatically moved.
