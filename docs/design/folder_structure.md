# Folder structure

The repository follows the root-level structure documented in README. New optimization levels should add separate configuration/spec/tests and reuse neutral schemas/loading/reporting where their contracts remain valid. They must not silently change Level 1 semantics.
